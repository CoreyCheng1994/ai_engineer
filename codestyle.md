在编写代码时，请以“可维护的工程代码”为目标，遵循以下约束：
- 设计上遵循 SOLID 等通用设计原则，避免不必要的复杂抽象
- 对重复或可复用逻辑进行封装，并以明确的接口暴露
- 保持代码高可读性：
  - 命名应准确反映意图
  - 函数保持短小且职责单一
  - 避免隐式副作用
- 以可测试性为设计前提：
  - 核心业务逻辑不依赖具体实现
  - 支持 Mock / Stub
  - 关键路径应易于编写单元测试
