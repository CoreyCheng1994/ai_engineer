使用说明：

1. 将项目clone到指定目录
   例如： codex: .codex （可以是局部 也可以是全局） 。然后重命名文件夹为 prompts
         cursor: .cursor/commands （可以是局部 也可以是全局）
         其他： 略

2. 复制 doc_loader 到 项目根路径下（ 也就是 codex 运行的目录），并更名为 doc_loader.md

当前只搭建了框架，后续会持续更新